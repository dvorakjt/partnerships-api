- Optimize query performance (ideal is < 50ms, < 100ms for medium complexity, < 200ms for max allowed complexity)
  - Join vs Nested Select
- TSDoc comments
- Testing
- Package Structure
- Docker Container Issues
- Securing it (api key or something required to access mutations, depth/expense limits)
- Updates to libraries:
  - Tests
  - TSDoc comments
  - Formatting
  - Publish w/ GH actions
  - Table valued functions